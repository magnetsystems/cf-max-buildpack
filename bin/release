#!/bin/bash
#
# Generate the default start command for this custom buildpack
# To use this command, set "command: null" in manifest.yml

cat <<EOF
---
default_procss_types:
  web: $1/max-server/bin/max-server.sh start
EOF
